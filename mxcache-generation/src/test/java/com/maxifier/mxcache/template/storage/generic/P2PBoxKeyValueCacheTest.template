/*
 * Copyright (c) 2008-2014 Maxifier Ltd. All Rights Reserved.
 */
package com.maxifier.mxcache.impl.caches.storage.generic;

import com.maxifier.mxcache.caches.*;
import com.maxifier.mxcache.storage.*;
import com.maxifier.mxcache.resource.MxResource;
import com.maxifier.mxcache.impl.MutableStatisticsImpl;
import com.maxifier.mxcache.impl.resource.DependencyTracker;
import com.maxifier.mxcache.impl.resource.ResourceOccupied;
import org.testng.annotations.Test;

import com.maxifier.mxcache.provider.Signature;
import com.maxifier.mxcache.impl.wrapping.Wrapping;

import static org.mockito.Mockito.*;

/**
 * #E##F#CacheBoxKeyValueTest
 *
 * THIS IS GENERATED CLASS! DON'T EDIT IT MANUALLY!
 *
 * GENERATED FROM #SOURCE#
 *
 * @author Andrey Yakoushin (andrey.yakoushin@maxifier.com)
 * @author Alexander Kochurov (alexander.kochurov@maxifier.com)
 */
@Test
public class #E##F#CacheBoxKeyValueTest {
    private static final #E##F#Calculatable CALCULATABLE = new #E##F#Calculatable() {
        @Override
        public #f# calculate(Object owner, #e# o) {
            assert o == #ETEST#;
            return #FTEST#;
        }
    };

    public void testMiss() {
        ObjectObjectStorage storage = mock(ObjectObjectStorage.class);

        when(storage.load(#ETEST#)).thenReturn(Storage.UNDEFINED);
        when(storage.size()).thenReturn(0);

        #E##F#Cache cache = (#E##F#Cache) Wrapping.getFactory(new Signature(Object.class, Object.class), new Signature(#e#.class, #f#.class), false).
                wrap("123", CALCULATABLE, storage, new MutableStatisticsImpl());
        cache.setDependencyNode(DependencyTracker.DUMMY_NODE);

        assert cache.getSize() == 0;
        assert cache.getStatistics().getHits() == 0;
        assert cache.getStatistics().getMisses() == 0;

        assert cache.getOrCreate(#ETEST#) == #FTEST#;

        assert cache.getStatistics().getHits() == 0;
        assert cache.getStatistics().getMisses() == 1;

        verify(storage).size();
        verify(storage, atLeast(1)).load(#ETEST#);
        verify(storage).save(#ETEST#, #FTEST#);
        verifyNoMoreInteractions(storage);
    }

    public void testHit() {
        ObjectObjectStorage storage = mock(ObjectObjectStorage.class);

        #E##F#Cache cache = (#E##F#Cache) Wrapping.getFactory(new Signature(Object.class, Object.class), new Signature(#e#.class, #f#.class), false).
                wrap("123", CALCULATABLE, storage, new MutableStatisticsImpl());
        cache.setDependencyNode(DependencyTracker.DUMMY_NODE);

        when(storage.load(#ETEST#)).thenReturn(#FTEST#);
        when(storage.size()).thenReturn(1);

        assert cache.getSize() == 1;
        assert cache.getStatistics().getHits() == 0;
        assert cache.getStatistics().getMisses() == 0;

        assert cache.getOrCreate(#ETEST#) == #FTEST#;

        assert cache.getStatistics().getHits() == 1;
        assert cache.getStatistics().getMisses() == 0;

        verify(storage).size();
        verify(storage, atLeast(2)).load(#ETEST#);
        verifyNoMoreInteractions(storage);
    }

    public void testClear() {
        ObjectObjectStorage storage = mock(ObjectObjectStorage.class);

        #E##F#Cache cache = (#E##F#Cache) Wrapping.getFactory(new Signature(Object.class, Object.class), new Signature(#e#.class, #f#.class), false).
                wrap("123", CALCULATABLE, storage, new MutableStatisticsImpl());
        cache.setDependencyNode(DependencyTracker.DUMMY_NODE);

        cache.clear();

        verify(storage).clear();
        verifyNoMoreInteractions(storage);
    }

    public void testSetDuringDependencyNodeOperations() {
        ObjectObjectStorage storage = mock(ObjectObjectStorage.class);

        when(storage.load(#ETEST#)).thenReturn(Storage.UNDEFINED, #FTEST#);

        #E##F#Calculatable calculatable = mock(#E##F#Calculatable.class);
        MxResource r = mock(MxResource.class);
        when(calculatable.calculate("123", #ETEST#)).thenThrow(new ResourceOccupied(r));

        #E##F#Cache cache = (#E##F#Cache) Wrapping.getFactory(new Signature(Object.class, Object.class), new Signature(#e#.class, #f#.class), false).
                wrap("123", calculatable, storage, new MutableStatisticsImpl());
        cache.setDependencyNode(DependencyTracker.DUMMY_NODE);

        assert cache.getStatistics().getHits() == 0;
        assert cache.getStatistics().getMisses() == 0;

        assert cache.getOrCreate(#ETEST#) == #FTEST#;

        assert cache.getStatistics().getHits() == 1;
        assert cache.getStatistics().getMisses() == 0;

        verify(storage, atLeast(2)).load(#ETEST#);
        verifyNoMoreInteractions(storage);
        verify(calculatable).calculate("123", #ETEST#);
        verifyNoMoreInteractions(calculatable);
    }

    public void testResetStat() {
        ObjectObjectStorage storage = mock(ObjectObjectStorage.class);

        when(storage.load(#ETEST#)).thenReturn(Storage.UNDEFINED);

        #E##F#Cache cache = (#E##F#Cache) Wrapping.getFactory(new Signature(Object.class, Object.class), new Signature(#e#.class, #f#.class), false).
                wrap("123", CALCULATABLE, storage, new MutableStatisticsImpl());
        cache.setDependencyNode(DependencyTracker.DUMMY_NODE);

        assert cache.getStatistics().getHits() == 0;
        assert cache.getStatistics().getMisses() == 0;

        assert cache.getOrCreate(#ETEST#) == #FTEST#;

        assert cache.getStatistics().getHits() == 0;
        assert cache.getStatistics().getMisses() == 1;

        cache.getStatistics().reset();

        assert cache.getStatistics().getHits() == 0;
        assert cache.getStatistics().getMisses() == 0;

        verify(storage, atLeast(1)).load(#ETEST#);
        verify(storage).save(#ETEST#, #FTEST#);
        verifyNoMoreInteractions(storage);
    }
}
